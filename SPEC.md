# Спецификация проекта: KAI Student Assistant (Группа 5108)

## 1. Профиль студента
- Вуз: КНИТУ-КАИ им. А.Н. Туполева
- Институт: ИРЭФ-ЦТ (Институт радиоэлектроники, фотоники и цифровых технологий)
- Академическая группа: 5108
- Подгруппа: 2 (Вторая подгруппа)

## 2. Архитектура потоков данных
1. Расписание занятий:
   - Источник: https://kai.ru/raspisanie (публичный JSON API)
   - Механизм: GET-запрос поиска `groupId` по имени "5108", затем POST-запрос сетки пар.
   - Фильтрация: отсекать занятия 1-й подгруппы (`1 п/г`, `1 подгр.`, `(1)`), оставлять общие пары потока и пары 2-й подгруппы (`2 п/г`, `2 подгр.`).
   - Учет четности недели (по ISO-календарю).

2. Личный кабинет Blackboard:
   - Источник: https://bb.kai.ru
   - Механизм: Headless Chromium (Playwright).
   - Авторизация с сохранением сессии в `storage_state.json`.
   - Обход списка курсов (`.portletList-img`), парсинг названий заданий, описаний дедлайнов и прямых ссылок на методички (`/bbcswebdav/`).

3. Мониторинг Telegram-чатов (Zero-Bot-in-Chat):
   - Механизм: Telethon (Userbot под личным профилем студента). В беседы сторонние боты не добавляются.
   - Отслеживаемые чаты:
     1. Чат группы без кураторов (повышенный уровень флуда).
     2. Чат группы с кураторами (официальные распоряжения, деканат).
     3. Супергруппа/Форум ИРЭФ-ЦТ: строго топик `General` и топик `5108` (остальные ветки чужих групп игнорируются).
   - Анализ текста: Google GenAI SDK (`gemini-3.5-flash` / `gemini-3.5-flash-lite`) со Structured Outputs (Pydantic-схема `ExtractedTask`).
   - Отсечение флуда, учет подгрупп (если задание адресовано только 1-й подгруппе — игнорировать).

4. Система упреждающих уведомлений (APScheduler):
   - За 3 дня до даты пары с лабораторной (18:00): фаза старта (предупреждение, чтобы начать заранее).
   - Накануне пары (19:00): фаза проверки и распечатки отчетов.
   - Утром в день занятий (07:30): сводка с номерами зданий, аудиторий и списком необходимых материалов.
   - Фоновый запуск парсера Blackboard: каждые 4 часа.

5. Пользовательский интерфейс:
   - Личный Telegram-бот на `aiogram 3`.
   - Карточки подтверждения найденных ИИ задач с инлайн-кнопками: `[📥 В дедлайны]`, `[❌ Скип]`.
   - Кнопки под напоминаниями: `[✅ Отметить сданной]`.
   - Меню: «Расписание на сегодня/завтра», «Несданные хвосты», «Радар недели».

## 3. Стек технологий
- Python 3.11+
- aiogram 3.x, SQLAlchemy 2.0 (asyncio + aiosqlite), SQLite
- requests, aiohttp, playwright, beautifulsoup4
- telethon, google-genai, pydantic, pydantic-settings
- apscheduler, python-dotenv, loguru